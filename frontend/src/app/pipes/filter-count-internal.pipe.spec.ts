import { FilterCountInternalPipe } from './filter-count-internal.pipe';

describe('FilterCountInternalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCountInternalPipe();
    expect(pipe).toBeTruthy();
  });
});
